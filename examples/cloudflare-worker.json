{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Example: Cloudflare Worker using full preset, just the defaults",
  "extends": [
    "@gfmio/config-renovate:presets/full/cloudflare-worker"
  ]
}
